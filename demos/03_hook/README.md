# 03. Hook 데모

> **상태:** 자리만 잡아둔 상태. 실제 데모 콘텐츠는 다음 단계에서 채웁니다.

## 이 데모가 보여줄 것

- Hook은 **AI의 판단 없이** 라이프사이클 이벤트(파일 저장, 툴 호출 등)에 결정론적으로 끼어드는 스크립트
- "이건 무조건 일어나야 한다"는 규칙을 코드로 박아둘 때 쓴다는 것
- LLM에게 "잊지 말고 해줘"라고 부탁하는 것과는 본질적으로 다르다는 것

## 시연 위치

`.claude/settings.json`의 `hooks` 섹션에 정의합니다.
